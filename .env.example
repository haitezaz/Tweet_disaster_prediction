
# Firebase Configuration
FIREBASE_PROJECT_ID=project_id
FIREBASE_LOCATION=location
DATA_CONNECT_SERVICE_ID=service_id
DATA_CONNECT_CONNECTOR_ID=connector_id

# Firebase Credentials (paste the raw JSON content here, or use GOOGLE_APPLICATION_CREDENTIALS path)
FIREBASE_CREDENTIALS_JSON='{"type": "service_account", ...}'
